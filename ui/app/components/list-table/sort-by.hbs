{{!
  Copyright IBM Corp. 2015, 2025
  SPDX-License-Identifier: BUSL-1.1
~}}

<th title={{@title}} class="is-selectable {{if this.isActive "is-active"}} {{if @sortDescending "desc" "asc"}}" ...attributes>
  <SafeLinkTo
    @query={{hash sortProperty=@prop sortDescending=this.shouldSortDescending}}
    data-test-sort-by={{@prop}}>
    {{yield}}
  </SafeLinkTo>
</th>
